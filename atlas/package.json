{
	"name": "atlas",
	"version": "0.1.0",
	"private": true,
	"type": "module",
	"scripts": {
		"dev": "bun --watch src/index.ts",
		"start": "bun src/index.ts",
		"typecheck": "tsc --noEmit",
		"build:types": "tsup src/index.ts --dts-only --out-dir ../packages/api-types"
	},
	"dependencies": {
		"gtfs-realtime-bindings": "^1.1.1",
		"hono": "^4.6.14"
	},
	"devDependencies": {
		"@types/bun": "^1.1.14",
		"tsup": "^8.5.1",
		"typescript": "^5.7.3"
	}
}
